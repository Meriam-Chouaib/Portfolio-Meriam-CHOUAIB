import { InfoUserProps } from 'pages/SignupPage/InfoUserForm/InfoUserForm.type'
import React from 'react'

function InfoUserForm({
  oldStepsRecord,
  onNextStep,
  onPreviousStep,
}: InfoUserProps) {
  return <div>InfoUserForm</div>
}

export default InfoUserForm
