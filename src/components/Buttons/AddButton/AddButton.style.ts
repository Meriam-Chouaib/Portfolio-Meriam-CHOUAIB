import { Stack } from '@mui/material'
import { styled } from '@mui/system'
import AddCircle from 'assets/icons/add_circle.svg?react'

export const AddFileStyle = styled(Stack)({
  cursor: 'pointer',
  paddingTop: 0,
})

export const AddCircleStyle = styled(AddCircle)({
  marginTop: 'auto',
  marginBottom: 'auto',
})
