import { auth } from 'locales/en/auth'
import { common } from 'locales/en/common'
import { header } from 'locales/en/header'
import { home } from 'locales/en/home'

const en = { lng: 'en', home, header, common, auth }
export default en
